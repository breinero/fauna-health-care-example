Patient.all().forEach( 
  patient => { Credential.create( { document: patient, password: 'changeme' }) }
);

Practitioner.all().forEach( 
  practitioner => { Credential.create( { document: practitioner, password: 'changeme' }) }
);

Insurer.all().forEach( 
  insurer => { Credential.create( { document: insurer, password: 'changeme' }) }
);